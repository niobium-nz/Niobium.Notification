{
  "contentVersion": "1.0.0.0",
  "$schema": "https://azure.com",
  "parameters": {
    "environmentName": {
      "value": "${AZURE_ENV_NAME}"
    },
    "AZURE_RESOURCE_GROUP": {
      "value": "rg-niobium-notify-${AZURE_ENV_NAME}"
    }
  }
}
